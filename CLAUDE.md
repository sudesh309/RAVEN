# CLAUDE.md

## Git workflow

- Prefer working directly on `main` rather than creating a new feature branch
  per session. Only create a separate branch when explicitly asked to, or when
  a change is risky enough to want an isolated PR.
